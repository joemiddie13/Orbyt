{
  "name": "orbyt-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
